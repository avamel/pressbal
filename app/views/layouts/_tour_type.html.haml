- if @tour_types.present?
  .tour-type
    %h2Популярные виды отдыха
    %ul
      - @tour_types.each do |type|
        - if type.tours.present?
          %li= link_to type.title,  tours_path(type: type.slug), title: "#{type.title}"