- if @countries.present?
  .country-block
    %ul
      - @countries.each do |country|
        %li
          %span.flag{country: "#{country.title}"}
          = link_to country.title, tours_path(country: country.id), title: "#{country.title}"
