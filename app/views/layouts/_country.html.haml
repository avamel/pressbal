- if @countries.present?
  .country-block
    %ul
      - @countries.each do |country|
        - if country.tours.present?
          %li=link_to country.title, tours_path(country: country.slug), title: "#{country.title}", :style => "background: url('#{country.flag.url(:small)}') no-repeat;"
